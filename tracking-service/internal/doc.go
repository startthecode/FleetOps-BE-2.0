// Package internal holds the private application code for tracking-service.
// Planned sub-packages: handler (HTTP/WebSocket), kafka (consumers),
// cache (Redis), store (Postgres history).
package internal
